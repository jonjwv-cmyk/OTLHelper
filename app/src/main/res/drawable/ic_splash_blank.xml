<?xml version="1.0" encoding="utf-8"?>
<!--
  Splash icon placeholder: a vector whose fill is exactly the splash background
  color. Android 12+ SplashScreen API requires a non-null icon drawable, but we
  never want the launcher icon visible — this drawable renders as a solid block
  of bg_app on bg_app, so the result is the plain background with no icon.

  IMPORTANT: keep 108×108dp (adaptive-icon size). A 1dp vector gets replaced by
  the system default launcher icon on some OEM skins.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#111111"
        android:pathData="M0,0h108v108h-108z" />
</vector>
